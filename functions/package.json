{
  "name": "functions",
  "description": "Cloud Functions for rush-n-relax",
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "test": "vitest --run",
    "serve": "npm run build && firebase emulators:start --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^7.2.5",
    "tsx": "^4.21.0"
  },
  "devDependencies": {
    "@types/node": "^20.19.39",
    "typescript": "^5.3.3",
    "vitest": "^1.1.0"
  }
}
