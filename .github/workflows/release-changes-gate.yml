# Shared release-relevant changes gate
# - Reused by PR and merge workflows to avoid filter drift
name: Release Changes Gate

on:
  workflow_call:
    outputs:
      should_run:
        description: 'Whether release-relevant files changed'
        value: ${{ jobs.changes.outputs.should_run }}

jobs:
  changes:
    name: changes
    runs-on: ubuntu-latest
    outputs:
      should_run: ${{ steps.filter.outputs.release_relevant }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Detect release-relevant changes
        id: filter
        uses: dorny/paths-filter@v3
        with:
          filters: |
            release_relevant:
              - 'src/**'
              - 'functions/**'
              - 'e2e/**'
              - 'firebase.json'
              - 'apphosting.yaml'
              - 'next.config.ts'
              - 'firestore.rules'
              - 'firestore.indexes.json'
              - 'storage.rules'
              - 'package.json'
              - 'package-lock.json'
              - 'functions/package.json'
              - 'functions/package-lock.json'
              - 'tsconfig.json'
              - 'functions/tsconfig.json'
              - '.github/actions/**'
              - '.github/workflows/main-verify-deploy.yml'
              - '.github/workflows/pr-verify.yml'
